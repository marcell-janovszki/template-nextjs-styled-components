// STYLED-COMPONENTS
import styled from "styled-components"

const Wrapper = styled.div``

const HomePage = () => {
    return (
        <Wrapper>
            home_page
        </Wrapper>
    )
}

export default HomePage
